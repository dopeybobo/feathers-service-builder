{
  "name": "feathers-service-builder",
  "version": "0.1.0",
  "description": "A TypeScript friendly service builder for FeathersJS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublishOnly": "npm run build",
    "build": "tsc"
  },
  "peerDependencies": {
    "feathers": "^2.1.7"
  },
  "devDependencies": {
    "typescript": "^2.4.2"
  }
}
